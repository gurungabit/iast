# ============================================================================
# Frontend Environment Variables
# ============================================================================
# Copy this file to .env.local and update values for your environment.
# These variables are prefixed with VITE_ to be exposed to the client.

# API Configuration
# -----------------
# In development: Leave empty to use Vite proxy (/api -> localhost:3001)
# In production: Set to your API Gateway or backend URL
# Examples:
#   - Same origin: /api (if frontend and API are on same domain)
#   - API Gateway: https://api.example.com
#   - Cross-origin: https://api.yourdomain.com
VITE_API_BASE_URL=

# WebSocket Configuration
# -----------------------
# In development: Leave empty to use current host
# In production: Set to your WebSocket endpoint
# Examples:
#   - Same origin: wss://yourdomain.com
#   - Separate: wss://ws.yourdomain.com
VITE_WS_BASE_URL=

# Terminal Configuration (optional)
# ---------------------------------
VITE_TERMINAL_COLS=80
VITE_TERMINAL_ROWS=24
VITE_TERMINAL_FONT_SIZE=14
VITE_TERMINAL_FONT_FAMILY="Menlo, Monaco, 'Courier New', monospace"
VITE_TERMINAL_SCROLLBACK=10000
